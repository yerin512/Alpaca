<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.member">
	<resultMap id="memResult" type="memberVO">
		<result property="id" column="id" />
		<result property="pwd" column="pwd" />
		<result property="name" column="name" />
		<result property="email" column="email" />
		<result property="profile_img" column="profile_img"/>
	</resultMap>


	<insert id="insertMember" parameterType="memberVO">
		<![CDATA[
		 insert into trip_member(id, pwd, name, email, profile_img)
		 values(#{id}, #{pwd}, #{name}, #{email}, #{profile_img})
		]]>
	</insert>
	
	  <select id="loginById"  resultType="memberVO"   parameterType="memberVO" >
	<![CDATA[
		select * from trip_member	
		where id=#{id} and pwd=#{pwd}		
	]]>
  </select>
  
    <!-- 카카오 아이디 중복 체크  -->
  	  <select id="kIdChk" resultType="String"   parameterType="String" >
	<![CDATA[
		select * from trip_member	
		where id=#{id}	
	]]>
  </select>
  
  
  <!-- 아이디 중복 체크  -->
  	<select id="idChk" parameterType = "String" resultType = "String">
	   select nvl(count(*), 0)
	   from trip_member
	   where id = #{id}
	</select>

</mapper>